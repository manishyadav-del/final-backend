const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const subscribers = await prisma.subscriber.findMany(); console.log('Total subscribers:', subscribers.length); console.log(subscribers); const sites = await prisma.site.findMany(); console.log('Available sites:', sites); } main().catch(console.error).finally(() => prisma.\());
